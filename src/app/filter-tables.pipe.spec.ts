import { FilterTablesPipe } from './filter-tables.pipe';

describe('FilterTablesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterTablesPipe();
    expect(pipe).toBeTruthy();
  });
});
